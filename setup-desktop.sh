#!/usr/bin/env bash

# install mysql
# info "isntall mysql"
# sudo apt -q install -y --no-install-recommends mysql-server

# sudo apt install nvidia-cuda-toolkit # the nvidia driver will be replaced.

# google-chrome

# telegram

# edge

sudo apt -q install -y --no-install-recommends \
    anki \
    copyq \
    mpv \
    peek \
    flameshot

# then restart
